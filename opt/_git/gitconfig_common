[user]
  signingkey = 7CE1B930FCA3AB168E900C3336F8E00498CAB9F2
[core]
  autocrlf = input
  editor = kak
  pager = delta
[alias]
  br = branch
  cm = commit
  co = checkout
  cu = !git commit --date="$(date --utc +%Y-%m-%dT%H:%M:%S%z)"
  df = diff
  dfa = diff --unified=1000
  dfs = diff --stat
  lo = log --graph --pretty=format:'%C(cyan)%h %C(yellow)%ad %C(green)%an %C(reset)%s%d'
  lof = log -p --
  ls = ls-files
  nr = name-rev
  rb = rebase
  rl = reflog
  rs = reset
  s  = status --branch --short
  st = status
[delta]
  commit-decoration-style = 7 ol
  file-added-label = +
  file-copied-label = C
  file-modified-label = M
  file-removed-label = -
  file-renamed-label = R
  file-decoration-style = 7 ol
  file-style = 15 0
  hunk-header-decoration-style = none
  hunk-header-style = syntax 15
  hunk-label = ""
  line-numbers = true
  line-numbers-left-format = "{nm:>1}│"
  line-numbers-left-style = 7
  line-numbers-right-format = "║{np:>1}│"
  line-numbers-right-style = 7
  navigate = true
  side-by-side = true
[diff]
  colorMoved = default
[interactive]
  diffFilter = delta --color-only
[log]
  date = format:%y-%m-%d %H:%M:%S
[merge]
  conflictstyle = zdiff3
